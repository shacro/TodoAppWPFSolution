<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="RoundListBox" TargetType="ListBox">
        <Setter Property="SelectionMode" Value="Extended"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBox">
                    <Border CornerRadius="20"
                        BorderThickness="1"
                        BorderBrush="Black"
                        Background="#272B2F"
                        Padding="0,10,0,0">
                        <ScrollViewer Style="{DynamicResource modScrollviewer}" Focusable="False" CanContentScroll="False">
                            <VirtualizingStackPanel IsItemsHost="True"/>
                        </ScrollViewer>
                    </Border>

                </ControlTemplate>
            </Setter.Value>
        </Setter>

        <Setter Property="ItemContainerStyle" Value="{DynamicResource ItemStyle}"/>

    </Style>

    <Style TargetType="ListBoxItem" x:Key="ItemStyle">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Border x:Name="itemBorder"  Background="Transparent" VerticalAlignment="Top" Margin="0,0,0,0" Padding="0,5,0,5">
                        <TextBlock FontSize="20" Background="Transparent" Foreground="White"
                                   Text="{Binding Path=.}" Margin="10,0,0,0"/>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="itemBorder" Property="Background" Value="#212529"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

    </Style>

</ResourceDictionary>