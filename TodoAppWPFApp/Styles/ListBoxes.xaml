<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="RoundListBox" TargetType="ListBox">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBox">
                    <Border CornerRadius="20"
                        BorderThickness="1"
                        BorderBrush="Black"
                        Background="#272B2F">
                        <ScrollViewer Style="{DynamicResource modScrollviewer}" Focusable="False">
                            <VirtualizingStackPanel IsItemsHost="True"/>
                        </ScrollViewer>
                    </Border>

                </ControlTemplate>
            </Setter.Value>
        </Setter>

        <Setter Property="ItemContainerStyle" Value="{DynamicResource ItemStyle}"/>

    </Style>

    <Style TargetType="ListBoxItem" x:Key="ItemStyle">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <TextBox FontSize="17" Background="Transparent" Foreground="White"
                             IsReadOnly="True" BorderThickness="0" Text="{Binding Path=.}" Margin="5,0,0,0"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>